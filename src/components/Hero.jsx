import React, { Component } from 'react';

const imgHero = './images/hero.jpg';
const Hero = () => {
   
    return (
        <section className="hero">
                <img src={imgHero} alt=""/>
        </section>
    );
  
}

export default Hero;